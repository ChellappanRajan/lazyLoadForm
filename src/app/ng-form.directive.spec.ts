import { NgFormDirective } from './ng-form.directive';

describe('NgFormDirective', () => {
  it('should create an instance', () => {
    const directive = new NgFormDirective();
    expect(directive).toBeTruthy();
  });
});
